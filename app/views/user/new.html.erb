<%= form_for @user, url: user_index_path do |form| %>
  <h4 class="text-center">Sign Up</h4>
  <%= form.text_field :email %>
  <%= form.select :raffle_types_id, RaffleType.all.map{|c| [c.name, c.id]}, {}, {} %>
  <%= form.submit %>
<% end %>